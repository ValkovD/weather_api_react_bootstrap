import React, { Fragment } from 'react';
import loading from '../loading/loading-bar2.gif';
const Loading = () => {
  return (
    <div>
      <Fragment>
        <img src={loading} alt="Loading" />
      </Fragment>
    </div>
  )
}

export default Loading
